h1. arc_redirect

This is a Textpattern plugin for handling URL redirects. It adds an admin interface for defining redirects to avoid delivering 404s to site visitors.

h2. Requirements

Textpattern 4.5+